// Base URL for the API you want to query
const APIbase = "https://www.loc.gov/search/";

// URL to have query appended to. Used to link out to human readable search results
const querystem = "https://www.loc.gov/search/?q=";

// URL to have dates appended to. Used to link out to human readable search results
const datestem = "https://www.loc.gov/search/?dates=";

// URL for type searches to have type appended to. Does not include query param
const typestem = "https://www.loc.gov/";
const oftypestem = "https://www.loc.gov/search/?fa=original-format:";

// query param for human readable search
const firstqueryparam = "/?q=";
const laterqueryparam = "&q=";
